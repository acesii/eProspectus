package eprospectus

class Course {

    String courseCode
    String courseName

    Set subjects

    static hasMany = [subjects:CourseSubject]
    static mappedBy = [subjects:'course']

    static constraints = {
    }
}
